[package]
name = "m5rcode-runtime"
version = "0.1.0"
edition = "2021"
authors = ["m5rcode contributors"]
description = "Runtime library for m5rcode"
license = "MIT OR Apache-2.0"

[lib]
name = "m5rcode_runtime"
path = "src/lib.rs"

[dependencies]
